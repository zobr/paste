<?php

namespace app;

use \Psr7Middlewares\Middleware;

// Application middleware

// e.g: $app->add(new \Slim\Csrf\Guard);

// $app->add(function ($req, $res, $next) {
//     $auth_config = $app->config->get('auth');
//     $auth_mw = Middleware::BasicAuthentication($auth_config);
//     $auth_mw($req, $res, $next);
// });
